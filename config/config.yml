soc:
  name: "SOC Platform"
  version: "1.0.0"
  environment: "development"

logging:
  level: "INFO"
  file: "logs/soc_platform.log"

collection:
  interval: 5
  batch_size: 1000

alert:
  threshold: 60
  anomaly_threshold: 0.7

incident:
  critical_threshold: 80
  auto_response: true
